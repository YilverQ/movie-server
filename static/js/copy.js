const tagUrls = document.querySelectorAll(".COPYHIDDEN");
const copyURL = document.querySelectorAll(".copyURL");
console.log(tagUrls);
console.log(copyURL);

const hola = () =>{
	console.log("Se Ejecutó");
}

copyURL.forEach((img) => {
	copyURL.addEventListener("click", "hola");
});

for (i, j inf )

tagUrls.for (var i = Things.length - 1; i >= 0; i--) {
	Things[i]


}